Readme for <%= Name %>
<%= '=' * (Name.Length + 11) %>

To compile
----------
From the project base directory, run:
    nant

To test
-------
From the project base directory, run:
    nant test

To run builtin server
---------------------
Under Mono *nix system you need to have XSP2 installed and in the path.
From the project base directory, run:
    script/server

