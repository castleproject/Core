<?xml version="1.0" encoding="utf-8" ?> 

<configuration>

	<facilities>
		<facility id="logging">
			
			<default 
					enableInterception="true" 
					type="NLog | Log4net | Custom" 
					custom="typename, assemblyname" <!-- Only required if type="Custom": Must implement ILoggerFactory -->
					/>
		</facility>
	</facilities>

</configuration>